/**
 * Loading component for Settings page
 * Shows loading state while fetching settings data server-side
 */

import { SettingsLoadingState } from '@/components/shared';

export default function SettingsLoading() {
  return <SettingsLoadingState />;
}
