/**
 * Services feature types.
 * Re-export from business-profile for service shape; extend locally as needed.
 */

import type { BusinessServiceRow } from '@/features/business-profile/types/businessProfile';

export type { BusinessServiceRow };
