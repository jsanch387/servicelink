/* eslint-disable @typescript-eslint/no-explicit-any */
/** Service-role / session client without fighting incomplete generated table types. */
export function adminDb(client: unknown): any {
  return client;
}
