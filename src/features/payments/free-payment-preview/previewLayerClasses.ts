/**
 * Shared “glimpse behind the lock” styling for free-tier payments previews.
 * Single source of truth for dimmed content + scrim overlay.
 */
export const FREE_PREVIEW_DIMMED_CONTENT_CLASSES =
  'pointer-events-none select-none opacity-[0.58] sm:opacity-[0.62]';

export const FREE_PREVIEW_SCRIM_CLASSES =
  'pointer-events-none absolute inset-0 bg-neutral-950/28 sm:bg-neutral-950/24';
