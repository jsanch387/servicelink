/** Columns mapped onto `DashboardQuote` for owner list + detail. */
export const DASHBOARD_QUOTE_SELECT = `
          id,
          status,
          source,
          customer_name,
          customer_email,
          customer_phone,
          service_name,
          price_cents,
          duration_minutes,
          created_at,
          updated_at,
          scheduled_date,
          scheduled_start_time,
          note,
          request_message,
          vehicle_year,
          vehicle_make,
          vehicle_model,
          assets,
          customer_street_address,
          customer_unit_apt,
          customer_city,
          customer_state,
          customer_zip,
          service_address,
          service_id,
          service_price_option_id,
          service_price_cents,
          addon_details,
          viewed_at,
          customer_reminder_sent_at
        `;
