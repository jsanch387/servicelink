/** Layout helpers for dashboard GlassCard wrappers */

export const dashboardGlassCardLayout =
  'flex h-full flex-col min-h-[140px] lg:min-h-[156px]';

/** Subtle bordered look for in-card actions (use with shared Button + variant="ghost") */
export const dashboardCardButtonClass =
  'border border-white/[0.08] !bg-white/[0.03] !text-zinc-200 !font-medium !shadow-none hover:!border-white/15 hover:!bg-white/[0.06] hover:!text-white focus:!ring-white/20 active:!scale-100';
