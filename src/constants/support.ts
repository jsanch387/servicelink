/** Public support address (footer, contact page, legal, emails). */
export const SERVICELINK_SUPPORT_EMAIL = 'support@myservicelink.app' as const;

export const SERVICELINK_SUPPORT_MAILTO =
  `mailto:${SERVICELINK_SUPPORT_EMAIL}` as const;

/** Legal entity name for copyright / footer. */
export const SERVICELINK_LEGAL_NAME = 'ServiceLink LLC' as const;
