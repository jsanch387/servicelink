/** ServiceLink support inbox (footer, contact page, legal, dashboard). */
export const SERVICELINK_SUPPORT_EMAIL = 'app.servicelink@gmail.com' as const;

export const SERVICELINK_SUPPORT_MAILTO =
  `mailto:${SERVICELINK_SUPPORT_EMAIL}` as const;
